#include "schedule.h"
#include "json/json.h"

void generate_schedule(Json::Value &root, Schedule schedule) {
    // TODO: This should use the input in `root` to generate a schedule and output it to the `schedule` variable
}
